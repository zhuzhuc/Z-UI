# Frontend (React + Vite)

## Development

```bash
cd front
npm install
npm run dev
```

## Build

```bash
cd front
npm install
npm run build
```

Build output is in `front/dist`. Backend static serving now prefers `front/dist`.

